# GOAL BACKLOG

Stable-ID issue log. See `ORCHESTRATION.md` for the loop and status legend.
IDs are never reused. Add new issues at the bottom of the relevant domain section.

Format: `- [STATUS][Pxx] ID — short title | file:line | note`

---

## L. Design system / pills
- [FIXED][P2] L-001 — Canonize buttons as pills | client/src/components/ui/button.tsx | base+sm+lg changed rounded-md→rounded-full (session 1)
- [OPEN][P3] L-002 — Audit stray rounded-md/lg on raw <button> & Button className overrides across client/src; normalize to pill

## A. Auth & account lifecycle  (discovered session 1)
- [DONE][P0] A-001 — FIXED: Activation redirected to /login?activated=true but the login page ignored the param → user landed on a plain login form with zero feedback (dead-end). Client-side auto-login is impossible: the activate endpoint returns only `{success,message}` (no email/session token), so the honest fix is a success banner. Added `useSearchParams` to login-form.tsx; renders an "Your account has been activated! Please sign in with your new password." Alert when `?activated=true` (suppressed if an error is present). Removed the buggy `setMemberEmail(formData.activationToken)` placeholder in ActivateAccountForm.tsx. TDD: 2 banner tests in login-form.test.tsx (present/absent). | client/src/components/features/auth/login-form.tsx, client/src/app/activate-account/components/ActivateAccountForm.tsx
- [DONE][P1] A-002 — mid-session expiry now handled globally (commit 6af58a70). FINDINGS: auth is HttpOnly-cookie based, so getAuthToken()'s constant 'cookie-auth' is correct (not a bug — the browser attaches the cookie automatically). The real gap: cookie expires after 1h (30d w/ RememberMe) with NO refresh and NO global handling, so an expired session mid-use left the user in a broken still-"logged-in" UI (subsequent calls 401'd silently). FIX: apiClient now dispatches a SESSION_EXPIRED_EVENT on any 401 for a non-/auth/* request (via exported SSR-safe notifySessionExpired); AuthProvider listens and — only when a session is currently believed active (userRef guard avoids acting on unauthenticated background 401s) — clears session cache + user + Sentry context, sets a "session expired" error, and redirects to /login (unless already there). Full TDD coverage on helper + provider listener (80 tests pass; client gate green). Remaining refresh-token rotation work split to A-002b.
- [OPEN][P2] A-002b — no JWT refresh-token rotation (DISCOVERED via A-002) | backend AuthController.cs (no /auth/refresh) + client | Sessions hard-expire after 1h/30d; there is no sliding refresh, so active users are eventually force-logged-out. ACTION (larger, security-sensitive): add a refresh-token cookie + POST /auth/refresh with rotation & revocation, and have apiClient attempt a silent refresh on 401 before firing SESSION_EXPIRED_EVENT. Deferred — dedicated backend cycle; must not weaken existing auth security or break the 6151 backend tests.
- [DONE][P1] A-003 — Login mobile-client misclassification (commit f7387333) | backend AuthController.cs | Literal premise (web reads response.token) refuted: web uses HttpOnly cookie only (getAuthToken returns 'cookie-auth'). REAL bug: Login classified ANY UA containing "Mobile" as a native client, so iOS Safari / Android Chrome web users were given a body token but DENIED the HttpOnly cookie the web app depends on → sign-in silently broke on phones. Fixed to match only genuine native signals: X-Mobile-Client header OR ReactNative / GatherGrove-Mobile UA. +4 TDD tests (mobile-browser web, native-via-header, native-via-UA, desktop web). All 85 AuthController tests pass.
- [DONE][P1] A-004 — FIXED (dead-code removal, pairs with A-001): ActivateAccountForm destructured `{ login: _login } = useAuth()` with the comment "Extracted for future use, currently unused". A-001 established that client-side auto-login after activation is impossible (the activate endpoint returns only `{success,message}` — no email/session token), so this was permanently dead. Removed the destructure AND the now-unused `useAuth` import; also removed the now-dead `jest.mock('@/hooks/useAuth')` from the test (reduces mock count). 32 form tests + 21 page tests still pass. | client/src/app/activate-account/components/ActivateAccountForm.tsx, client/src/app/activate-account/components/__tests__/ActivateAccountForm.test.tsx
- [DONE][P2] A-005 — FIXED: resend-activation flow was unreachable/broken — `memberEmail` was only ever set to the activation *token* (the A-001 placeholder), there was no email input, and `handleResendActivation` always errored with "Please enter your email". Also found a UX bug: clicking "Request New Activation Link" ran `setGeneralError("")`, which unmounted the whole `{generalError && ...}` block (resend UI lived inside it) so the in-flight "Sending..." state and email field vanished. Fix: decoupled resend UI into its own `showResend` state (set when the activation error mentions expired/invalid), added a real email input bound to `memberEmail`, gated the resend button on a non-empty email. TDD: un-skipped + rewrote the 3 skipped describe blocks (Form Submission, Error Handling, Resend Activation = 10 tests) to mock apiClient (MSW does not intercept axios here) — 32 pass, 0 skip. | client/src/app/activate-account/components/ActivateAccountForm.tsx, client/src/app/activate-account/components/__tests__/ActivateAccountForm.test.tsx
- [OPEN][P2] A-006 — Redirect loop risk on auth pages w/ intent params | client/src/components/auth/RouteProtection.tsx:82
- [OPEN][P2] A-007 — Middleware doesn't validate JWT expiry | client/src/middleware.ts:24-30 | expired cookie → redirect then 401.
- [OPEN][P2] A-008 — LoginResponse no token for web fallback | backend AuthController.cs:214 / LoginResponse.cs:60

## B. Admin members  (discovered session 1)
- [DONE][P0] B-001 — engagementLevel filter rendered but never applied | client/src/app/admin/members/page.tsx | FIXED: load per-member overall scores via featureAnalyticsService.getMemberEngagementAnalytics into a Map, bucket by UI ranges (high>=80/medium 50-79/low 20-49/inactive<20) in applyColumnFilters; graceful no-op when engagement analytics unavailable. +4 tests.
- [DONE][P1] B-002 — customFieldService.ts (singular) DELETED: zero importers (grep confirmed), used non-existent `/custom-fields?clubId=` route (backend is `/clubs/{clubId}/custom-fields`). Live service is customFieldsService.ts (plural, 8 importers, correct route). Deleted file + its test.
- [DONE][P1] B-003 — custom-fields page missing tier gate | client/src/app/admin/members/custom-fields/page.tsx | FIXED (commit e6f6ea55): wrapped page in `<TierGate requiredTier="Grow" feature="custom-fields" showUpgrade>`. CORRECTION: backlog assumed Unlimited, but FeatureTiers in useTierValidation.ts lists `custom-fields` under Grow (line 233), so gate is Grow. Backend CustomFieldsController is `[Authorize]`-only (no tier policy) — client gate is the only enforcement today; a backend tier policy is a separate follow-up. Page body extracted to CustomFieldsPageContent; +1 gating test (asserts requiredTier/feature/showUpgrade props), existing 19 content tests still pass via passthrough TierGate mock.
- [OPEN][P2] B-004 — inviteCodeService unwraps .data.data | client/src/services/inviteCodeService.ts:65,89 | verify BE envelope shape
- [DONE][P3] B-005 — resolved by B-002: dead singular customFieldService.ts deleted, customFieldsService.ts is the single source.

## C. Admin events / QR
- [DONE][P1] C-001 — FIXED-by-removal (commit 25b93918): eventService.getEventPaymentLink (GET /clubs/{id}/events/{id}/payment-link) targeted a nonexistent endpoint — EventsController exposes ONLY POST {eventId}/payment-link (GeneratePaymentLink). The method had NO production consumer (admin event page derives the link from event.paymentToken). Removed the method + its test blocks across 3 eventService suites. | client/src/services/eventService.ts
- [DONE][P1] C-002 — FIXED-by-removal (commit 25b93918): eventService.revokePaymentLink (DELETE same path) also targeted a nonexistent backend endpoint and had no production consumer. Removed with its tests. generatePaymentLink (the real, wired POST consumed by admin/events/[eventId]/page.tsx) retained. | client/src/services/eventService.ts
- [OPEN][P2] C-003 — generateQRCode incomplete coverage | eventService | BE EventCheckinController only
- [OPEN][P2] C-004 — QR orphan endpoints (getEventQRCodes, downloadQRCode, getQRCodeShareUrl, updateQRCodeStatus, deleteQRCode) | eventService | BE: NONE
- [OPEN][P3] C-005 — bulkDownloadQRCodes orphan | eventService | BE: NONE
- [OPEN][P2] C-006 — validateQRCheckIn wrong route | eventService | should be /clubs/{clubId}/events/{eventId}/checkin/qr-code

## D. Payments / billing  (discovered session 1 — VERIFY D-002 before acting)
- [DONE][P0] D-001 — FALSE ALARM (backlog premise wrong, "BE: NONE" incorrect). The endpoint exists and is fully wired E2E: `UserController.PayMyDues` ([Route("api/v1/users")] + [HttpPost("me/dues/pay")] = `/api/v1/users/me/dues/pay`, matches client memberService.payMyDues). It pulls userId from JWT, validates ModelState, and calls `_memberService.PayMemberDuesAsync(userId, request)` (MemberService.cs:1005) which: finds the Member by user email, verifies MembershipTypeId match, guards on club.StripeAccountId (throws InvalidOperationException → 400 if club hasn't connected Stripe), reads DuesAmount, creates an internal PaymentToken, and processes a REAL Stripe charge via `_paymentService.ProcessPaymentAsync`, then returns a PaymentResponse. Dues are always paid in full (paymentAmount = MembershipType.DuesAmount), so IsPartialPayment=false / null balance fields is semantically correct per the DTO contract. No route conflict: MembersController.cs:617 declares the same verb under a different prefix ([Route("api/v1/clubs/{clubId}/members")]). | UserController.cs:581-650, MemberService.cs:1005-1099, PaymentResponse.cs
- [DONE][P3] D-002 — StripeService stub has NO live impact (downgraded P1→P3 after investigation). FINDINGS: IStripeService stub is registered in DI (ServiceCollectionExtensions.cs:27). Its ONLY production consumer is EventPricingService.cs, and specifically only the overloads `RegisterForPaidEventAsync(clubId, EventRegistrationRequestNew)` (line 155, uses _stripeService.CreatePaymentIntentAsync) and `ProcessEventRefundAsync(clubId, userId, EventRefundRequest)` (line 253, uses _stripeService.ProcessRefundAsync). NEITHER overload is wired to any controller — PaidEventsController calls the *different* overloads `ProcessEventPaymentAsync(ProcessEventPaymentRequest)` / `ProcessEventRefundAsync(ProcessEventRefundRequest)`, which don't touch IStripeService at all. So the IStripeService stub is reachable only via dead/legacy method overloads. The live, frontend-reachable event-payment paths are EventPaymentsController `/users/me/events/pay` and public `/public/events/pay` (real Stripe via PaymentService, per D-001) — they do NOT use IStripeService. No code change needed; implementing the stub would only affect unreachable code (and risk the 6151 tests that mock it). Real footgun split out as D-006.
- [OPEN][P2] D-006 — PaidEventsController payment/pricing/refund methods are pure stubs returning fake success (DISCOVERED via D-002) | backend EventPricingService.cs:507,519,534 + PaidEventsController.cs | `ProcessEventPaymentAsync` (519) returns a fabricated PaymentResponse (random PaymentId, no Stripe charge, no DB persistence); `CalculateEventPricingAsync` (507) returns hardcoded $10; `ProcessEventRefundAsync(ProcessEventRefundRequest)` (534) returns fake success. The controller is `[Authorize(Policy="ClubMember")]` at `api/v1/clubs/{clubId}/paid-events` and IS HTTP-reachable, but NO current web or mobile client calls it (web/mobile use EventPaymentsController instead). RISK: any future/API consumer hitting POST .../paid-events/payment gets 200 "succeeded" while NO money is collected and NO record is written. ACTION (large, test-heavy): either (a) implement these against the real PaymentService/Stripe path + persist EventRsvp like RegisterForPaidEventAsync does, or (b) if PaidEventsController is redundant with EventPaymentsController, remove it to eliminate the fake-success surface. Must not break the 6151 backend tests. Deferred — needs dedicated backend cycle + build/test budget.
- [DONE][P1] D-003 — VERIFIED GOOD (no change needed): EventPaymentsController is `[Route("api/v1/users/me/events")]` + `[HttpPost("pay")]` = `/api/v1/users/me/events/pay`. Client eventService.payForEvent posts bare `/users/me/events/pay` on the apiClient /api/v1 baseURL → exact match. Route resolves correctly. | EventPaymentsController.cs:13,52
- [DONE][P1] D-004 — Subscription-status reconciliation on renewal invoices (commit 6f7e49cf) | backend BillingService.cs. Premise partly refuted: invoice.payment_succeeded/failed ARE wired (switch lines 540-544), and customer.subscription.created is redundant because UpgradeSubscriptionAsync (253-258) + ClaimTrialAsync (409-413) persist club.Tier/Status/StripeSubscriptionId synchronously. REAL bug found (untested handlers): both invoice handlers resolved the subscription via line.Parent.InvoiceItemDetails.Subscription ONLY — populated for one-off invoice items, NOT subscription renewals. Stripe.net 48.2.0 puts a renewal's subscription on invoice.Parent.SubscriptionDetails.SubscriptionId and line.Parent.SubscriptionItemDetails.Subscription, so renewal invoices resolved to null → club status NEVER reconciled. Impact: a failed renewal never flipped the club to past_due (account-lock keys off SubscriptionStatus) and a recovered payment never restored active. FIX: pure ExtractSubscriptionIdFromInvoice helper (invoice-level → subscription line → legacy one-off), both handlers routed through it, +6 unit tests over the real Stripe model. Full backend gate green (3522+ tests).
- [OPEN][P2] D-005 — Coupon amount unit | backend BillingService.cs:647 | verify cents vs dollars to FE
- [OPEN][P2] D-011 — PayDues missing "stripe not configured" guard (was mis-numbered D-006, a duplicate ID; renumbered to D-011) | client PayDues.tsx:193-282
- [OPEN][P2] D-007 — Guest payment tier-gating gap | backend NonMemberEventPaymentService.cs:134 | paid events should require Grow+ tier
- [OPEN][P2] D-008 — Export endpoint no pagination | backend EventPaymentAdminController.cs:245 | OOM risk on large exports
- [OPEN][P3] D-009 — billingService relative baseUrl | client billingService.ts:85
- [OPEN][P3] D-010 — Redundant cents conversion | backend PaymentService.cs:295,371

## E. Communications  (discovered session 1)
- [DONE][P0] E-001 — resetForm() called undefined setWorkflowSteps → ReferenceError on every successful create, swallowed by catch into a false "Failed to create" error toast. FIX: removed the dead _workflowSteps/_setWorkflowSteps state (never read; create uses a locally-built defaultSteps) and the broken setter call in resetForm. TDD: added co-located test asserting success toast fires, NO error toast, and form resets. Resolves E-008 simultaneously. | client/src/app/admin/communications/workflows/page.tsx
- [OPEN][P1] E-002 — scheduled page loads empty mock data (TODO) | client/src/app/admin/communications/scheduled/page.tsx:80-86 | implement GET /clubs/{clubId}/communications/scheduled + service
- [OPEN][P1] E-003 — no schedule/delay option for any channel | client/src/app/admin/communications/new/page.tsx | add scheduledFor to SendBulkXxx + handler
- [DONE][P1] E-004 — FIXED: 8 of 10 methods passed the hardcoded context 'sendBulkEmail' to ErrorHandler, so SMS/WhatsApp/push/usage/history failures all surfaced "Error sendBulkEmail: ..." to users (ErrorHandler prefixes the message with `Error <context>:`). Gave each method its own context (getEmailUsageStats, sendBulkSms, getSmsUsageStats, sendBulkWhatsApp, getWhatsAppTemplates, sendPushNotification, getPushNotificationUsageStats, getCommunicationHistory). The pre-existing error tests only asserted `.rejects.toBeDefined()` — too weak to catch this — so added a 9-test "Error context labeling (E-004)" describe block asserting the correct per-method context appears in the thrown message. 48 tests pass. | client/src/services/communicationService.ts, client/src/services/__tests__/communicationService.test.ts
- [OPEN][P2] E-005 — SMS recipientCount hardcoded 0 (TODO) | client/src/app/admin/communications/sms/page.tsx:43-47 | wire memberService.getActiveMembers
- [OPEN][P2] E-006 — personalization tokens hardcoded array | email-templates/designer/page.tsx:91-100 | fetch from backend
- [OPEN][P2] E-007 — cancel scheduled handler is TODO, no API call | scheduled/page.tsx:103-105
- [DONE][P3] E-008 — unused _workflowSteps state | workflows/page.tsx | removed together with E-001 fix
- [OPEN][P3] E-009 — toggleWorkflow POSTs bare boolean body | communicationWorkflowService.ts:105-110 | wrap {isActive}
- [OPEN][P3] E-010 — unused handleRecipientCountChange callback | new/page.tsx:360-362

## F. Real-time / SignalR  (discovered session 1 — DEEP RE-AUDIT session 2)
> SESSION-2 FINDING: the SignalR layer is broken scaffolding, not a set of one-line typos. Only `/chatHub` (ChatHub) and `/eventEngagementHub` (EventEngagementHub) are mapped in Program.cs:1015-1016. There are TWO orphaned real-time features:
>   (1) AnalyticsHub.cs — fully implemented (JoinClubAnalytics, StreamEngagementMetrics/Cohort/ROI/Segmentation, events EngagementUpdate/CohortUpdate/ROIUpdate/SegmentationUpdate/AnalyticsError, [Authorize UnlimitedTierRequired]) but NOT mapped AND has ZERO frontend client. `TierAwareSignalRService.joinAnalyticsStream()` is mis-implemented — it calls `signalRService.joinClubChat()` (the CHAT hub), never AnalyticsHub (see F-010).
>   (2) Real-time MEMBER engagement — `useRealTimeEngagement.ts` (consumed by EngagementStatusBadge + RealTimeEngagementDashboard) connects to `/engagementHub` (does not exist) and invokes `JoinClubEngagement`/`LeaveClubEngagement`/`SubscribeToMemberEngagement`, listening for `EngagementStats`/`MemberEngagementUpdate`/`ClubStatsUpdate`/`AtRiskAlert`/`MemberActivity`/`JoinedEngagement`/`LeftEngagement`. NO backend hub implements this contract. The backend domain DOES exist (MemberEngagementService/Controller, entities MemberEngagementScore/Alert/History) but is REST-only.
> A token URL edit would falsely close F-002. The real fix is a backend feature build + a deliberate decision to keep or delete the FE scaffolding. Treat F-001/F-002 as feature work, not wiring nits.
- [DONE][P1] F-001 — (commit 0541e9c1) Backend half FIXED: AnalyticsHub is fully implemented and its services were registered via builder.Services.AddAdvancedAnalytics(...), but the endpoint mapping (UseAdvancedAnalytics() → app.MapHub<AnalyticsHub>("/hubs/analytics")) was NEVER invoked, so /hubs/analytics returned 404. RESOLUTION: chose option (a) — keep the feature (it is a complete, intentional Unlimited-tier real-time analytics hub) and invoke app.UseAdvancedAnalytics() alongside the other MapHub calls in Program.cs (~line 1017). Hub endpoint is now /hubs/analytics. Backend gate passed (3516 tests). The FE consumer half remains as F-010 (TierAwareSignalRService.joinAnalyticsStream must connect to /hubs/analytics instead of mis-joining the chat hub). | backend Program.cs:1017, backend AnalyticsHub.cs, client TierAwareSignalRService.ts
- [OPEN][P1] F-002 — MISSING BACKEND HUB (not a URL typo): `useRealTimeEngagement` (live, used by EngagementStatusBadge + RealTimeEngagementDashboard) targets `/engagementHub` which does not exist; its method/event contract matches NO backend hub. Backend member-engagement data exists via IMemberEngagementService (scores, at-risk alerts) but only over REST. Fix = build a new `MemberEngagementHub` ([Authorize], methods JoinClubEngagement/LeaveClubEngagement/SubscribeToMemberEngagement) wrapping IMemberEngagementService, broadcasting EngagementStats/MemberEngagementUpdate/ClubStatsUpdate/AtRiskAlert/MemberActivity to a club group; map at `/engagementHub`; add a push trigger (broadcast from MemberEngagementService score/alert mutations, or a hosted polling service); backend tests. Subsumes the old F-004 (JoinClubEngagement vs JoinClubEventEngagement) and F-008. Delegate as a backend feature (heavy dotnet test gate). | client/src/hooks/useRealTimeEngagement.ts:340,165 + (NEW) backend MemberEngagementHub.cs
- [OPEN][P2] F-003 — FE listens NewMessage but ChatHub never sends it | client/src/services/signalrService.ts:168 + ChatHub
- [DONE][P1] F-004 — SUBSUMED by F-002 (FE invokes JoinClubEngagement but no backend hub exists at all; the method-name mismatch is moot until the MemberEngagementHub is built per F-002). Reclassified P2→tracked-under-F-002.
- [OPEN][P2] F-005 — ChatHub.SendMessageToClub never called from FE; persistence unclear | backend ChatHub.cs:127
- [OPEN][P2] F-006 — missing accessTokenFactory (JWT) on hub connection | client/src/services/signalrService.ts:32
- [OPEN][P3] F-007 — inconsistent hub authorization policies | EventEngagementHub.cs:13 vs ChatHub
- [DONE][P3] F-008 — SUBSUMED by F-002 (engagement hook targets unmapped 'engagement' hub — same root cause as the missing MemberEngagementHub).
- [DONE][P2] F-010 — (commit 883bd48f) FIXED. Added a dedicated AnalyticsSignalRService (client/src/services/analyticsSignalRService.ts) that connects to the now-mapped /hubs/analytics endpoint and exposes the real hub contract: JoinClubAnalytics/LeaveClubAnalytics/RefreshAllAnalytics + on{Engagement,Cohort,ROI,Segmentation}Update / onAnalyticsError subscriptions. TierAwareSignalRService now owns a separate analytics connection and joinAnalyticsStream() invokes joinClubAnalytics (was: joinClubChat on the chat hub). Chat and analytics use distinct connections to their respective hubs; tier gating preserved. New analyticsSignalRService.test.ts (24 tests) + updated TierAwareSignalRService.test.ts (now asserts analytics hub is hit and chat hub is NOT). 74/74 pass; full client gate passed.
  > RESIDUAL (not a bug): no UI page currently subscribes to the analytics client — the wiring is correct and reachable end-to-end (FE client ⇄ /hubs/analytics ⇄ IAdvancedAnalyticsService), but a live analytics dashboard that consumes the stream is a separate UNBUILT feature, not in scope here. With F-001 (backend map) + F-010 (FE client) both done, the analytics real-time path is now correctly wired; building a consuming dashboard UI would be net-new feature work.

## G. Member portal /app  (discovered session 1 — verified, easy first batch)
- [FIXED][P2] G-001 — events card → "/events" should be "/app/events" | client/src/app/app/member-dashboard/page.tsx:77
- [FIXED][P2] G-002 — view-events btn → "/events" should be "/app/events" | member-dashboard/page.tsx:186
- [FIXED][P2] G-003 — back btn → "/member-dashboard" missing /app | client/src/app/app/profile/page.tsx:235
- [FIXED][P2] G-004 — cancel btn → "/member-dashboard" missing /app | profile/page.tsx:436
- [OPEN][P3] G-005 — two dashboards both export MemberDashboard | client/src/app/app/dashboard/page.tsx:12
- [OPEN][P3] G-006 — EventWithRsvp memberRsvp:any | dashboard/components/PaidEventsSection.tsx:61
- [OPEN][P3] G-007 — "Member Since" placeholder never populated | membership/page.tsx:145
- [FIXED][P2] G-008 — directory back nav ignores role | directory/page.tsx:119
- [OPEN][P3] G-009 — event detail back nav not context-aware | events/[eventId]/page.tsx:322

## H. Multi-location  (discovered session 1)
- [DONE][P0] H-001 — VERIFIED RESOLVED: route now exists at client/src/app/admin/locations/[locationId]/page.tsx with branding/ and transfers/ subroutes, all covered by passing tests (3 suites, 90 tests). settings/locations link (router.push(`/admin/locations/${id}`)) resolves correctly. | client/src/app/admin/locations/[locationId]/
- [DONE][P0] H-002 — FIXED: loadBranding() used a stale-closure `...formData` spread AND nested the entire setFormData inside `if (data.colorScheme)`, dropping logo/name whenever no colorScheme existed. Rewrote to parse colors into a local var (fallback {} on JSON.parse error) then ALWAYS call functional `setFormData((prev) => ({...prev, customLogoUrl, customNameOverride, primaryColor: colors.primary || prev.primaryColor, ...}))`. Logo/name now populate independent of colorScheme; colors fall back to defaults on malformed JSON. 2 TDD tests added (45 total pass). | client/src/app/admin/locations/[locationId]/branding/page.tsx
- [DONE][P1] H-003 — loadData error swallowed, no UI | client/src/app/admin/settings/branding/page.tsx | FIXED (commit 47c35fd4): (1) failed getBrandSettings load now sets a destructive Alert warning the shown values are defaults & to refresh before saving (was silently swallowed → risk of overwriting saved branding on save). (2) BONUS BUG: load useEffect depended on the whole `user` object → re-ran on every render that produced a new user ref, reloading settings over unsaved edits; narrowed dep to primitive `user?.clubId`. (3) TEST-QUALITY: this page's MSW handlers never matched (harness overrides global.fetch, doesn't intercept axios), so the suite only asserted default state; converted to boundary-mock brandingService/themeService deterministically (distinct "Loaded Club Name" proves load is actually exercised), +load-success/failure/no-settings coverage. 23/23 pass.
- [OPEN][P2] M-003 — page-level MSW handlers do not intercept axios (DISCOVERED via H-003) | client/src/mocks/server.ts:391-401 setupMswServer overrides global.fetch with createMockFetch (which ignores MSW http.* handlers); axios page requests therefore bypass MSW and fail/return nothing. Many `*page.test.tsx` suites using inline `server.use(http.get(...))` are effectively dead (they only assert default render state). ACTION: audit page tests for dead MSW handlers; either remove the global.fetch override so MSW intercepts both fetch+XHR, or standardize page tests on service-boundary mocks (as H-003/custom-fields do). NOTE the contradictory comment at setupTests.ts:35 ("do NOT override global.fetch") vs the actual override.
- [DONE][P1] H-004 — Create Location btn no loading/disabled state | settings/locations/page.tsx:315 | FIXED (commit dcfeebaa): handleCreateLocation had no in-flight guard → double-click could fire createLocation twice (duplicate locations). Added isCreating state: early-return when already creating, set flag in try / clear in finally; Create button now shows spinner + "Creating..." and is disabled while in flight; Cancel disabled while creating. TDD test added (never-resolving createLocation) asserting in-flight label, both buttons disabled, and that a 2nd click does not trigger a 2nd create. 43/43 pass.
- [OPEN][P2] H-005 — uses window.location.href not next/navigation | settings/locations/page.tsx:168
- [OPEN][P2] H-006 — no error-state UI on branding load | branding/page.tsx:18
- [OPEN][P3] H-007 — contactEmail no validation | settings/locations/page.tsx:284 | type=email + pattern

## I. Analytics / dashboard  (discovered session 1)
- [DONE][P0] I-001 — MemberEngagement paths missing /v1 (3 endpoints: trends/overview/history) — controller is at api/v1/MemberEngagement, client called /api/MemberEngagement → 404. FIX: prefixed all 3 with /api/v1. TDD: updated 3 path assertions in analyticsService.test.ts first. | client/src/services/analyticsService.ts:189,211,221
- [DONE][P0] I-002 — verify premium engagement-trends path/verb — VERIFIED CORRECT: AdvancedAnalyticsController is at `api/clubs/{clubId}/analytics/premium` (NO /v1) and exposes `[HttpGet("engagement-trends")]`; client path /api/clubs/{clubId}/analytics/premium/engagement-trends matches. No change needed. | analyticsService.ts:205
- [DONE][P1] I-003 — AtRiskMembersAlert no longer fabricates members. Removed the 5 hardcoded fake members (Jennifer Walsh et al.) and the `process.env.NODE_ENV==='test'`/`setTimeout` mock branch. NEW service `client/src/services/memberEngagementService.ts` wires GET /api/v1/MemberEngagement/club/{clubId}/at-risk?threshold=40 and maps backend MemberEngagementScore (+included Member) → AtRiskMember via pure exported helpers (classifyRiskLevel, deriveReasons from lowest sub-score, mapToAtRiskMember). Unknown fields (decline history, contributions, events, tier) use honest neutral defaults — NOT fabricated figures. Component now has real fetch + loadError state + error banner (role="alert", testid at-risk-load-error). Errors propagate via ErrorHandler.handleApiError({context:'getAtRiskMembers'}). Deleted redundant 1219-line AtRiskMembersAlert.enhanced.test.tsx (83 fabricated-data assertions, 0 real-backend coverage — pure mock-behavior test). Tests: memberEngagementService.test.ts 12 pass (apiClient boundary mock only); AtRiskMembersAlert.test.tsx rewritten to 18 pass driving real service+component through mocked HTTP boundary. Typecheck clean on touched files.
- [DONE-PARTIAL→SPLIT][P1] I-004 — RECONCILED. The honesty/wiring bug (service fabricated "sample" report data + export blobs on error) was fixed in task #9: all 4 methods (generateComprehensiveReport / exportAttendanceData / exportEngagementMetrics / exportMemberParticipation) now propagate a normalized ErrorHandler error and MUST NOT fabricate data. Residual reality: backend exposes NO `/api/v1/events/{clubId}/reports/*` endpoints (grep-confirmed: only CrossLocationReportsController `api/v1/clubs/{clubId}/reports` and ExportController scheduled-reports exist, neither matches), AND the sole consumer `EventReportsPanel.tsx` is ORPHANED (imported by no route/page — grep confirmed). So this is a fully UNBUILT feature on both ends, not a live regression — no admin can currently reach the panel. Feature build split to I-004b. | client/src/services/eventReportsService.ts
- [OPEN][P2] I-004b — NET-NEW FEATURE (split from I-004, downgraded from P1: not a live bug — panel is unreachable). Build the event-reports backend (4 endpoints: POST events/{clubId}/reports/comprehensive returning ReportData; POST reports/attendance|engagement|members/export returning a real file blob) assembled from existing analytics/engagement services (NO fabricated figures), then wire the orphaned EventReportsPanel.tsx into an admin analytics route. Large, test-heavy (dotnet gate + export-format generation). Deferred to a dedicated feature cycle. | (NEW) backend EventReportsController + client/src/components/analytics/events/EventReportsPanel.tsx route wiring
- [DONE][P1] I-005 — analyticsExportService no longer fabricates data on error. All 6 API methods now `throw ErrorHandler.handleApiError(error, {context})` instead of silently returning mock metrics/blobs (removed getMock*/createMock* helpers). Consumers (DataExportCenter, LazyDataExport) already have try/catch+toast, so failures now surface honestly. ALSO fixed wiring: added missing `exportEventAnalytics`/`exportEngagementData` methods that LazyDataExport called but didn't exist, and fixed LazyDataExport's financial case calling non-existent `exportFinancialSummary` → `exportFinancialData` with proper FinancialExportOptions. Tests: 77 pass (error cases now assert rejection; +5 new-method tests).
- [OPEN][P2] I-006 — daysBack hardcoded 30 | analyticsService.ts:189 | parameterize
- [OPEN][P2] I-007 — duesService is ENTIRELY mock (returns mock-transaction) | client/src/services/duesService.ts | implement real backend wiring (overlaps D-001)
- [OPEN][P2] I-008 — engagement page gated Unlimited-only but data avail all tiers | client/src/app/admin/engagement/page.tsx:102
- [OPEN][P2] I-009 — FeatureAdoption=null dead code in export | backend EngagementController.cs:184
- [OPEN][P2] I-010 — placeholder transform methods (weekly/monthly/csv/excel) | backend EngagementController.cs:245-309 | implement
- [OPEN][P3] I-011 — eventReportsService silent mock fallback | eventReportsService.ts:82
- [OPEN][P3] I-012 — hardcoded peak-time helpers | backend EngagementController.cs:271-279

## J. Public marketing site / honesty rule  (discovered session 1)
- [DONE][P1] J-001 — FIXED (honesty rule): the hero stat "40,000+ Words of Content" had no data source (no word counts exist anywhere on the page or in the guides array). Replaced with "4+ Hours of Expert Reading" — verifiable: the per-guide `readTime` values shown on every card sum to ~243 min (>4 h). Updated both stats assertions in page.test.tsx (incl. queryBy not-present guards for the old strings). | client/src/app/resources/page.tsx, client/src/app/resources/__tests__/page.test.tsx
- [DONE][P1] J-002 — FIXED (honesty rule): removed the hardcoded "Updated January 2026" line on the featured-guide card — there is no last-updated tracking, so any date is fabricated. Test now asserts no "Updated <month year>" text renders. | client/src/app/resources/page.tsx, client/src/app/resources/__tests__/page.test.tsx
- [OPEN][P2] J-003 — "8-14 hours/month" stat needs source link | blog/_posts/real-cost-of-spreadsheets.tsx:66
- [OPEN][P2] J-004 — "53% of nonprofits" NTEN stat unlinked | blog/_posts/paper-to-digital-transition.tsx:52
- [OPEN][P2] J-005 — "20-35% higher attendance" ASAE unlinked | blog/_posts/club-annual-calendar-template.tsx:20
- [OPEN][P2] J-006 — email open-rate stats unlinked | blog/_posts/club-newsletter-tips.tsx:26-28
- [OPEN][P2] J-007 — "8,000+ words" guide claim unverified | resources/page.tsx:308
- [OPEN][P3] J-008 — pricing messaging on resources hero | resources/page.tsx:206
- [OPEN][P3] J-009 — support email monitored-note | support/page.tsx:59
- NOTE: marketing lead form correctly wired to /api/v1/marketing/leads; calculators functional; CTAs OK.

## K. Cross-cutting wiring (apiClient ↔ backend routes)
_(partial — see C/D above; full sweep of all ~60 services pending)_

### W-series — apiClient ↔ backend route reconciliation (session 2/3)
Root cause class: apiClient axios baseURL already = `http://localhost:8050/api/v1`; axios CONCATENATES baseURL+url, so any client path beginning `/api/v1/...` becomes `.../api/v1/api/v1/...` (DOUBLED_PREFIX, always 404). Correct client paths must NOT include `/api/v1`.
- [DONE][P0] W-001a — FIXED: memberEngagementService doubled /api/v1 prefix. Corrected to bare `/MemberEngagement/...` paths. | client/src/services/memberEngagementService.ts (commit ac60aba6)
- [DONE][P0] W-001b — FIXED: accountDeletionService was unusable E2E — (1) all 7 paths doubled (/api/v1/api/v1/...) → 404; (2) request body omitted backend-required `confirmationPhrase` and sent fields the API never reads (confirmDataExport/confirmUnderstanding/password) → 400; (3) response interfaces did not match real DTOs; (4) two client-only endpoints (export-data, confirm) the backend never exposed. Corrected all paths to bare `/account-deletion/...`; rebuilt AccountDeletionRequest to mirror the real DTO (reason, confirmationPhrase, requestDataExport, transferClubOwnershipToUserId, memberDataHandling, clubTransferInstructions, deleteOrphanedClubs, passwordConfirmation); realigned all response interfaces; removed the 2 phantom endpoints (grace-period model is the intended design). Both dialogs collapsed to validation→confirmation→processing with the "type DELETE MY ACCOUNT" gate, blob export download, GUID-threaded cancel, and onAccountDeleted firing only after a successful initiate. Tests: 38 pass across 3 suites. | client/src/services/accountDeletionService.ts, client/src/components/account/AccountDeletionDialog.tsx, client/src/components/admin/AdminAccountDeletionDialog.tsx (+3 test files) (commit 5406c5d0)
- [DONE][P0] W-002 — FIXED: analyticsExportService routing mismatch. The premium analytics controller is `[Route("api/clubs/{clubId}/analytics/premium")]` (no /api/v1), so apiClient's /api/v1 baseURL doubled to 404. Decision: reach the real routes via absolute host-base URLs (`process.env.NEXT_PUBLIC_API_URL || http://localhost:8050`), matching analyticsService's proven raw pattern — NOT a backend v1-standardization (too large a blast radius). Changes: (1) exportAnalyticsData + exportCustomMetrics now run the REAL POST→GET-blob flow (POST ExportRequestDto {dataType,format,startDate,endDate} → {downloadUrl,filename} → GET blob), both return Promise<Blob>; JSON rejected up front (backend emits csv/excel/pdf only); UI category mapped to backend dataType (members→segmentation, growth/retention→cohorts). (2) getEngagementAnalytics → bare `/MemberEngagement/club/{id}/overview` (genuinely under /api/v1). (3) getMemberAnalytics→.../premium/segmentation, getGrowthMetrics→.../premium/cohorts (absolute). (4) Deleted getEventAnalytics (no backend GET endpoint; EventAnalytics type retained for CSV helper). (5) DataExportCenter drops json from analytics format selector; LazyDataExport coerces json→csv for analytics quick-exports. BACKEND BUG also fixed: AdvancedAnalyticsService.ExportDataAsync returned a DownloadUrl missing `/premium/` → 404; corrected + test strengthened. Verified: client jest 72 pass, tsc clean on touched files, eslint 0 errors; backend build 0 errors, full solution test green (3516 Application + 2116 API). | client/src/services/analyticsExportService.ts, client/src/components/unlimited/LazyDataExport.tsx, client/src/components/unlimited/export/DataExportCenter.tsx, backend/src/GatherGrove.Application/Services/AdvancedAnalyticsService.cs (+2 test files) (commits 43bba314 backend, 26baecfa client)
- [DONE][P1] BE — FIXED (discovered while running the full backend gate for W-002): RateLimitingMiddleware XFF "bug-finding" tests asserted per-X-Forwarded-For-IP rate-limit separation but never enabled proxy trust, so RequestClientIpResolver (secure-by-default — only honors XFF when TrustedProxy is set) collapsed both forwarded IPs to the shared RemoteIpAddress bucket → GetClientIP_DifferentForwardedIPs_RateLimitedSeparately failed (a pre-existing red on main, masked because prior runs only exercised the Application.Tests project). Middleware behavior is correct; fixed the test setup to enable TrustedProxy so all three XFF scenarios exercise the real trusted-proxy path. | backend/tests/GatherGrove.API.Tests/Middleware/RateLimitingMiddlewareTests.cs (commit 858b090e)
- [DONE][P0] W-003 — FIXED: bulkOperationsService was far more broken than the original one-line entry implied. Investigation against BulkOperationsController + 10 DTOs revealed the whole service was a fabricated/generic surface (executeBulkOperation, retryBulkOperation, getBulkOperationHistory, getBulkOperationMetrics, estimateProcessingTime, getBulkOperations, validateBulkOperation, executeBulkOperationGeneric, exportMembers) pointing at NONEXISTENT routes (`/api/bulk-operations/*`, `/clubs/{id}/members/bulk`, `/api/members/export`) — every call 404'd — plus the two real status/cancel calls had REVERSED route order (`/{id}/status`, `/{id}/cancel` vs the backend's `status/{id}`, `cancel/{id}`). Rewrote the service to faithfully mirror the 8 real endpoints under `/clubs/{clubId}/bulk-operations` (assign-tags, remove-tags, update-custom-fields, update-member-statuses, export, import, GET status/{id}, POST cancel/{id}), all relative to the apiClient /api/v1 baseURL (no doubled prefix). Encoded all 7 request enums as NUMERIC ordinals (BulkExportFormat, ImportFileType, TagRemovalMode, CustomFieldUpdateMode, ImportMode, MemberMatchCriteria, BulkOperationStatus) to match the backend's integer enum serialization — Program.cs sets CamelCase + case-insensitive but registers NO JsonStringEnumConverter. Rebuilt request/response interfaces from the DTOs (clubId/requestedByUserId omitted — server-set). Kept the Unlimited-tier gate, member-id bounds (1..10000), and status cache (terminal-status TTL). Deleted 9 dead methods + their phantom routes. Rewrote the test suite (27 tests) asserting real routes, numeric enums, tier gate, validation guards, status caching/invalidation, and ErrorHandler context propagation; boundary-only mocks (apiClient, billingService, ErrorHandler — the last reapplied per-test because jest.config resetMocks:true wipes factory impls). Verified: client gate green (lint→typecheck→27 tests→build). | client/src/services/bulkOperationsService.ts (+test) (commit 67f99d5e)
- [OPEN][P1] W-003b — ORPHANED/UNWIRED bulk-operations + segmentation feature (discovered during W-003): bulkOperationsService has ZERO production consumers. Both `BulkOperationsPanel.tsx` are non-functional: `client/src/components/features/members/segmentation/BulkOperationsPanel.tsx` uses a LOCAL fabricated `mockBulkOperationsService` (lines ~32-130) via React Query (never calls the real service); `client/src/components/members/segmentation/BulkOperationsPanel.tsx` is a stub returning "Component under development" with `any`-typed stubs. Neither panel — nor the wider segmentation feature tree — is imported by any app route. Needs: wire a single real panel to bulkOperationsService (8 ops), delete the mock/stub duplicates, and surface it under an admin members/segmentation route. | client/src/components/**/segmentation/BulkOperationsPanel.tsx
- [DONE][P1] W-004 — FIXED: segmentAnalyticsService doubled /api/v1 prefix. baseUrl was '/api/v1/clubs' on an apiClient (baseURL already /api/v1) → /api/v1/api/v1/clubs/... 404. All 11 methods map to real SegmentAnalyticsController actions (insights, segments/{id}, growth-analysis, engagement-trends, export, compare) under [Route("api/v1/clubs/{clubId}/segment-analytics")] — only the prefix was wrong. Corrected baseUrl→'/clubs', updated 13 test path assertions. 35 tests pass. Consumed by SegmentAnalytics.tsx. | client/src/services/segmentAnalyticsService.ts (+test) (commit 2766e420)

#### W-misc audit results (session 3 — full audit, task #10 done). apiClient baseURL=/api/v1; raw `fetch('/api/...')` is RELATIVE → hits the FRONTEND origin (localhost:3050), and next.config.ts has NO /api→backend rewrite, so all raw-fetch services are also transport-broken in local dev (and rely on same-origin proxying in prod). Each below is consumed by real UI (not orphaned).
- [DONE][P1] W-005 — loginActivityService (apiClient). FIXED (commit cf4669a3). BASE_URL '/api/v1/analytics/login-activity' → '/analytics/login-activity' (apiClient already carries /api/v1). Removed the 5 fabricated NO_BACKEND_ENDPOINT methods (summary/daily/hourly/devices/geographic) — no backend route and no data source for session duration, peak hour, device class, or geographic data (honesty rule). Reworked useLoginActivity to source data solely from the real stats/{clubId} endpoint (LoginActivityStats incl. loginTrends with web/mobile split) and dropped the (LoginActivityService as any) casts. Reworked LoginActivityDashboard to render only truthful metrics (total/active/monthly/inactive, daily login trends, real web-vs-mobile chart) and wired the period selector to the hook. Service/hook/dashboard tests rewritten to the new surface (boundary mocks only, 64 tests pass); client gate (lint/typecheck/2000+ tests/build) green. | client/src/services/loginActivityService.ts
- [DONE][P1] W-006 — eventEngagementApiService transport FIXED (backend commit 706caa1e, client commit a811805c). The 7 REAL methods were wrong on BOTH axes: backend EventEngagementController was [Route("api")] (no v1) so unreachable via apiClient's /api/v1 baseURL, AND the client prefixed every path with a bogus `/EventEngagement` segment the controller never had. Chose the canonical re-mount over an absolute host-base call: backend [Route("api")] → [Route("api/v1")] (verified no collision — EventsController is api/v1/clubs/{clubId}/events, MobileAnalyticsController uses events/{eventId}/engagement-analytics); client stripped `/EventEngagement` from the 7 real paths (getEventEngagement→/events/{id}/engagement, getClubEventsEngagement→/clubs/{id}/events/engagement, getEventAnalyticsDashboard→/events/analytics, getMemberEventEngagement→/members/{id}/events/engagement, recordEventAttendance→POST /events/{id}/attendance, submitEventFeedback→POST /events/{id}/feedback, getEventRecommendations→/events/recommendations/{id}). 33 service tests updated/green; backend build 0 errors + full backend test gate green; client gate (lint→typecheck→tests→build) green. Consumed by EventEngagementDashboard.tsx + useEngagement.ts. | client/src/services/eventEngagementApiService.ts + backend EventEngagementController.cs
- [DONE][P1] W-006b — FIXED (commit 2b62a538): reconciled eventEngagementApiService to the real EventEngagementController contract. The service was almost entirely disconnected from its backend — not just 13 fabricated extra methods, but ALL 7 "real" methods had wrong response interfaces and getEventAnalytics would throw against the live API. (1) Removed 13 fabricated methods (getEventAttendance, getEventFeedback, exportAnalyticsReport, getMemberEngagementBreakdown, getComparativeAnalysis, getCategoryAnalytics, getPredictiveAnalytics, getEventById, getFeedbackTemplate, getFeedbackAnalytics, exportFeedback, getFeedbackTimeline, sendFeedbackReminders) + 9 orphaned interfaces + unused imports. (2) Rewrote all 7 real method response interfaces to match the backend DTOs EXACTLY (camelCase): EventEngagementMetricsResponse, ClubEventEngagementResponse, EventEngagementTrendsResponse, DailyEventEngagementResponse, MemberEventEngagementItem, EventAttendanceResponse, MemberEventEngagementHistoryResponse, EventAnalyticsResponse, EventFeedbackResponse, EventRecommendationResponse; trimmed request bodies (RecordEventAttendanceRequest, SubmitEventFeedbackRequest) to real fields. (3) getEventAnalytics now composes getEventAnalyticsDashboard + getClubEventsEngagement with HONEST neutral defaults (eventSatisfactionScore=0, repeatAttendanceRate=0, noShowRate=0, averageRating=0, revenueGenerated=0, upcomingEvents=[], attendanceData category=''/eventType='other') — REMOVED all Math.random()/magic-number synthesis (honesty rule). (4) EventEngagementDashboard shows 'N/A' for backend-unavailable metrics (Satisfaction Score, Repeat Attendance). (5) useEngagement.fetchMemberEngagement rewired to real DTO fields (no Math.random). (6) Replaced stub `expect(x).toBeDefined()` contract tests with real contract tests; rewrote service + hook tests. Verified: client gate green (lint 0 errors/1 pre-existing `any` warning, typecheck pass, 123 tests, build 265 pages). | client/src/services/eventEngagementApiService.ts + useEngagement.ts + EventEngagementDashboard.tsx (+4 test files)
  - [OPEN][P2] W-006d — useEngagement.fetchClubEngagement still fabricates data: monthlyTrends via Math.random and segmentation/activityBreakdown via magic numbers, sourced from featureAnalyticsService (out of W-006b scope). Needs backend support or honest UI simplification per the honesty rule. | client/src/hooks/useEngagement.ts (fetchClubEngagement)
  - [OPEN][P3] W-006e — pre-existing tsc errors in useEngagement test files (SendOutreachResponse.failedCount in useEngagement.test.ts:766/809 + useEngagement.real.test.tsx:694; EngagementTimeRange '1y'/'30d' in useEngagement.real.test.tsx:151/604) — present on main before W-006b, jest/build-tolerated, low priority. | client/src/hooks/__tests__/useEngagement.test.ts + useEngagement.real.test.tsx
- [DONE][P1] W-007 — tagService reconciled to the real club-scoped MemberTaggingController. Rewrote tagService.ts: raw `fetch('/api/v1/tags...')` → apiClient; removed the global `/tags` collection + tag-category concept (NO backend support); remapped every method onto MemberTaggingController's [Route("api/v1/clubs/{clubId}/members/tags")] contract — getTags `/clubs/{id}/members/tags`, getTag/{tagId}, createTag (POST), updateTag (PUT /{tagId}), deleteTag (DELETE /{tagId}), assignTag (POST assign/{memberId}/{tagId}), removeTag (DELETE remove/{memberId}/{tagId}), getMemberTags (member/{memberId}), getMembersWithTag ({tagId}/members), getTagUsageStats (usage-stats); int IDs throughout; kept the bulk UX via client-side fan-out helpers assignTagsToMembers/removeTagsFromMembers (Promise.all over member×tag → single-pair endpoint). Removed the mockTagService silent fallback (honesty-rule violation). Reworked its only (orphaned) consumer features/members/segmentation/TagManager.tsx: added required `clubId` prop, dropped categories UI, numeric selection, stats from currentStats. Rewrote both test files to apiClient boundary mock with exact path assertions (54 tests green). Client gate (tests→typecheck→lint) green. | client/src/services/tagService.ts + features/members/segmentation/TagManager.tsx
- [OPEN][P2] W-007b — the ENTIRE members segmentation UI is orphaned: NO app route renders TagManager, SegmentBuilder, SegmentAnalytics, AdvancedMemberFilter, CustomFieldManager, BulkOperationsPanel, or SegmentCommunications. Both segmentation component trees are reachable only from their own __tests__. Needs: wire the canonical segmentation surface into a real members route (or formally decide it is deferred). | client/src/components/features/members/segmentation/*
- [DONE][P1] W-007c — FIXED (commit 2d96646a): canonicalized the members-segmentation UI on `components/features/members/segmentation/` and DELETED the older orphaned duplicate `components/members/segmentation/` (6 components + 6 test files). Verified the older dir had ZERO production/route consumers (grep of src/app and all of src found no importer of `components/members/segmentation`) — reachable only from its own __tests__ — and the canonical dir is the one whose types.ts segmentService/segmentAnalyticsService import and whose TagManager W-007 reconciled to tagService. No unique behavior lost (canonical dir is a superset: adds SegmentCommunications + types.ts). | client/src/components/members/segmentation/* (deleted)
- [DONE][P1] W-007d — FIXED (commit 2d96646a): DELETED memberTaggingService.ts (+test) rather than rewire. Confirmed its ONLY consumer was the (now-deleted) older `components/members/segmentation/TagManager.tsx`; the canonical `features/.../TagManager.tsx` uses the W-007-fixed tagService. Every memberTaggingService path was mis-wired vs MemberTaggingController (`/clubs/{id}/tags*` vs the real `/clubs/{id}/members/tags*`, plus a NO_BACKEND_ENDPOINT `/tags/bulk`), so deletion is the honest resolution. ALSO pruned the stale MSW tag handlers from src/mocks/handlers.ts: the `${API_BASE}/clubs/:clubId/tags*` family (matched memberTaggingService's wrong paths) and the `/api/v1/tags`, `/api/v1/tags/categories`, `/api/v1/tags/stats`, `/api/v1/tags/assign|remove`, `/api/v1/tags/categories` global family (matched the pre-W-007 global-tags surface that no longer exists). Both matched neither the real backend nor any boundary-mocked test. Verified: canonical features/ segmentation + tagService suites green (211 passed); full client gate green. | client/src/services/memberTaggingService.ts (deleted) + src/mocks/handlers.ts
- [DONE][P1] W-008 — FIXED: memberDataExportService rewritten from raw fetch (relative '/api' → frontend origin in dev) onto the REAL ExportController via apiClient + absolute host base (`process.env.NEXT_PUBLIC_API_URL || http://localhost:8050`, the W-002 pattern). ExportController is the only [Route("api")] controller and exposes EXACTLY three member-export ops: POST clubs/{clubId}/members/export → GET clubs/{clubId}/members/export/{exportId}/status → GET clubs/{clubId}/exports/{exportId}/download. Enums cross the wire as integer ordinals (Program.cs registers CamelCase but NO JsonStringEnumConverter), so format is encoded numerically (csv=0, excel=1, pdf=2, json=3) and ExportStatus normalized 0-5→string union. Removed the fabricated job-queue/cancel/history surface (getExportJob, cancelExport, getExportHistory, deleteExportHistory + ExportJob/ExportHistoryItem types + the stale manual service mock). Reconciled all 6 consumers honestly: ExportJobTracker polls getExportStatus(clubId,exportId) (cancel removed); MemberExportDialog runs export→poll-status→download (fabricated job-id panel removed); LazyDataExport polls status instead of getExportJob; DataExportCenter tracks session-local export history + polls status; ExportHistoryPanel + ExportHistoryViewer converted to pure prop-driven session-history components (no server fetch, honestly labelled "Recent Exports (this session)"). Tests rewritten to apiClient boundary mocks against the real contract — 94 pass across 5 suites. Client gate green (lint→typecheck→tests→build). | client/src/services/memberDataExportService.ts + ExportJobTracker/ExportHistoryPanel/MemberExportDialog + unlimited/{LazyDataExport,export/DataExportCenter,export/ExportHistoryViewer} (+5 test files, -1 manual mock) (commit ff271041)
- [OPEN][P2] W-008b — ORPHANED export UI (discovered during W-008): all 6 member-export components (ExportHistoryPanel, ExportJobTracker, MemberExportDialog, DataExportCenter, ExportHistoryViewer, LazyDataExport) have ZERO production route consumers — reachable only from their own __tests__. Needs: wire the canonical export surface (likely MemberExportDialog + DataExportCenter) into a real admin members/export route, or formally defer. | client/src/components/**Export*, client/src/components/unlimited/**
- [OPEN][P2] W-008c — MISSING BACKEND FEATURE (not fabricated, recorded honestly): ExportController has no export job-queue (async/cancel) and no persistent server-side export history. The client now tracks history session-locally only. If durable export history + cancellable async exports are desired, build it server-side first (DB-backed export records + background processing + endpoints: list history, cancel job), TDD with 95% coverage, then re-point the client. | backend ExportController + Application export service
- [DONE][P1] W-009 — financialExportService reconciled to real ExportController (commit 1de2b83d). Rewired onto apiClient + absolute host base (NEXT_PUBLIC_API_URL||http://localhost:8050). Kept ONLY the real POST /api/clubs/{clubId}/financial/export (Format encoded as integer ExportFormat ordinal; returns JSON ExportResult synchronously with Status=Completed, NOT a blob) + added downloadFinancialExport via the shared GET /api/clubs/{clubId}/exports/{exportId}/download blob route. Removed the 6 NO_BACKEND_ENDPOINT methods (financial/export/async, financial/summary, billing/export, events/revenue/export, financial/tax-report/{taxYear}, financial/categories), all mock-data generators, and fabricated option fields (streamingExport/currencyConversion/includeCharts/includeSummaryTotals). Kept pure helpers (validate, generateFinancialCSV, calculateMetrics) with real coverage. Reconciled FinancialExportDialog/LazyDataExport/DataExportCenter to the honest export→download-on-completed flow (error surfaced on non-completed). Rewrote FinancialExportDialog test to render the REAL component (was a self-mocked stub). Deleted stale unused manual mock. Full client gate green.
  - [OPEN][P2] W-009b — degraded/orphaned financial-export UI: backend financial export has no status-polling endpoint and is Admin-only synchronous; the `includeProjections` UI toggle and `groupBy: member|event` options map to nothing in the backend FinancialExportRequest (only groupByCategory exists). Either implement backend support or trim the UI toggles. Also verify FinancialExportDialog/DataExportCenter/LazyDataExport have real production route consumers (as with W-008b).
  - [OPEN][P3] W-009c — missing backend FinancialSummary/category/tax-report endpoints. If a financial dashboard summary or tax report is desired, build the server-side endpoints first (TDD), then add typed client methods — do NOT re-introduce client-side mock generators.
- [DONE][P2] W-010 — memberDirectoryService routed through apiClient (commit a9663923). Switched GET/PUT from raw fetch (relative '/api/v1' → frontend origin in dev) to apiClient with the bare '/users/me/directory-settings' path (matches backend UserDirectorySettingsController). Errors normalized to preserve backend/network messages with a fallback. Test rewritten to boundary-mock apiClient. Consumer app/app/profile/page.tsx unaffected (mocks the service). Full client gate green.
- [NOTE] Test-only dead candidates still to triage: membershipService, stripeService, directoryService, memberFilterService.

## M. Tooling / quality-gate blind spots  (discovered session 2)
- [OPEN][P1] M-001 — `npm run build` does NOT catch TS/lint errors: next.config.ts sets `typescript.ignoreBuildErrors: true` AND `eslint.ignoreDuringBuilds: true` | client/next.config.ts:19-29 | this let E-001's undefined-identifier bug ship to runtime
- [OPEN][P1] M-002 — `npm run typecheck` only covers src/services|types|lib|utils; src/app/**, src/components/**, src/hooks/** are NEVER typechecked | client/tsconfig.typecheck.json | systemic: app-page reference/type errors (like E-001) are invisible to all gates. Extend include to src/app + src/components + src/hooks (expect a backlog of latent errors to surface).
  - **Audit (session 3+): ran full `tsc -p tsconfig.json` → 69 latent errors across 27 files (saved to /tmp/m002-errors.txt). Fixing by severity before flipping the gate. Gate-flip itself BLOCKED until layout.tsx ThemeProvider error (belongs to uncommitted dark-mode refactor) is triaged.**
  - **M-002 runtime-crash undefined-identifier bugs (catch blocks / refs) — FIXED:**
    - [DONE] EventInvitationDialog.tsx — `request` block-scoped in try, referenced in catch's logger → ReferenceError swallowed every send failure (commit 8d3eecd1, +regression test)
    - [DONE] EventQRCodeGenerator.tsx — 3 catch blocks referenced undefined qrType/formData, qrCodeId, activeQRs → ReferenceError before toast/log (commit 56007f7c, +3 regression tests)
  - **M-002 wrong-cased / wrong-typed property reads (silent undefined) — FIXED:**
    - [DONE] useRealTimeEventEngagement.ts — 6 logger.debug calls read camelCase off PascalCase SignalR payloads + .length on non-arrays (commit 54a0fed5)
    - [DONE] onboarding/page.tsx(x2), profile/page.tsx (visible Account ID blank), events/pay/[token]/page.tsx, communications/scheduled/page.tsx — `.id`/`.eventId` off wrong types (commit 9e8e8a8d)
  - **M-002 remaining (type-strictness, mostly non-crash) — OPEN:** chart-lib prop/label shapes (EventAttendanceChart, EngagementTrendsChart period, FeatureUsageAnalytics/EventAttendanceChart PieLabel percentage), sitemap.xml changeFrequency literal, popover/tooltip/form-field cloneElement overloads, AdvancedInsightPanel `.id` keys, ROITracker null deref (346/348) + insight `.message` (921), PricingSection `.replace` on never (411), sso-buttons email-not-in-SSOLoginRequest (71) + props any (209), usePerformanceOptimization/useRealTimeAnalytics "Expected 1 arg", signalr-connection overload, BrandAssetManager "Expected 1 arg", DataExportManager Date-as-key, communications/new FormErrors index, useRealTimeEngagement.ts 11 EventHandler<unknown> (F-002 orphaned hook), EventReportsPanel `options` (I-004b orphaned), layout.tsx ThemeProvider (BLOCKED, dark-mode refactor).

## N. Test debt (skipped suites masking faulty test code)  (discovered session 2)
- [DONE][P1] N-001 — FIXED: 5 `describe.skip` blocks (19 tests) in settings/locations were skipped for fake "flaky/data rendering" reasons; root cause was faulty TEST code, not production: (a) useToast mock returned `{toast: mockToast}` but real hook returns `{success,error}` directly → every toast path threw; (b) email/phone queried by exact string but rendered with emoji prefix in same node; (c) deactivate-button scoped via `.closest('.space-y-3')` (CardContent) but title is in CardHeader → null, masked by silent `if (btn)` guards. Un-skipped all 5, fixed mock + selectors (`[data-slot="card"]`), removed null-guards (assertions now stronger). 42 pass, 0 skip. page.tsx unchanged (no production bug). | client/src/app/admin/settings/locations/__tests__/page.test.tsx
- [DONE][P1] N-002 — FIXED: 4 describe.skip + 1 it.skip (19 tests) in transfers test were disabled for fake "flaky dialog" reasons. Same useToast mock-shape bug as N-001 (`{toast: mockToast}` vs real `{success,error}`) made every approve/deny/error path throw. Also fixed 3 latent test bugs the skip had hidden: (a) approve-dialog `getByText(/john doe/i)` ambiguously matched both the dialog description and the name div → switched to exact `'John Doe'`; (b) "empty notes for approval" expected `approvalNotes: ''` but component sends `notes.trim() || undefined` → corrected to `undefined`; (c) "show error if notes empty on deny" clicked a button that is `disabled` while notes empty (handleDeny's toast guard is unreachable from UI) → rewrote to assert the real UX (Deny stays disabled until a reason is typed). 49 pass, 0 skip. page.tsx unchanged. | client/src/app/admin/locations/[locationId]/transfers/__tests__/page.test.tsx
